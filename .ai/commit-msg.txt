fix(quality): batch 8 - hotkeys, form validation, code quality rules

P1 fixes:
- Wire up useGlobalKeyboardActions in ToastProvider via GlobalKeyboardActions
  component (Ctrl+Z/S now functional: undo/redo shows "not supported" toast,
  Ctrl+S prevented from browser save dialog)
- Add name validation to 3 forms (CharacterEditor, SceneEditor, StoryHeader):
  save button disabled when name/title is empty

P2 fixes:
- Extract setTimeout magic numbers to timers.ts constants (14 replacements
  across 10 files: BLOB_URL_REVOKE_DELAY_MS, COPY_RESET_DELAY_MS, etc.)
- Add 5 eslint code quality rules (max-lines, max-lines-per-function,
  max-params, max-depth, complexity) as warn level
- Unify error-codes naming to uppercase (ERROR_CATEGORY_MAP keys)
  with toUpperCase() fallback in classifyError for API compatibility
- Remove unused ApiResponse.ok field (redundant with success)

Tests: 4661 passed. typecheck + lint:arch all clean.
