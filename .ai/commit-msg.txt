refactor(debt): batch 6 - comprehensive debt cleanup

1. StoryBeat deprecated field cleanup:
   - Remove scene field (18 fallbacks -> sceneId, DB already migrated)
   - Remove generationPrompt field (2 UI fallbacks, 3 explicit undefined)
   - Fix imageGenerationPrompt annotation (was wrongly marked deprecated)
   - Remove 7 deprecated fallback compatibility tests

2. shared/ui directory cleanup (8 files):
   - Delete 4 dead code files (feedback/input-group/loading-state/status-badge)
   - Replace confirm-dialog with imperative confirm() (3 beat-editor files)
   - Migrate 3 active components to shared/presentation/ (AppCard/EmptyState/SafeImage)
   - shared/ui/ directory deleted entirely

3. Dead code cleanup:
   - tones.color field (completely unused, deleted)
   - useHomePage exportAllData/downloadExportMutation (settings page has export)

4. Route handler as assertion elimination:
   - Eliminate 5 assertions (Cause D: API response type 4 + Cause C: no index signature 1)
   - Add TextApiResult type to narrow generateText return
   - Use z.record().parse() instead of as unknown as Record
   - 21 retained assertions annotated with reasons

Tests: 4661 renderer + 952 electron all pass
