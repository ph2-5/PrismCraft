refactor: unify character fields and remove legacy migration code

Breaking changes (no users affected):
- Remove `character` and `characters` from StoryBeat schema, unify to `characterIds`
- Simplify `getBeatCharacterIds()` to just return `beat.characterIds ?? []`
- Update all read/write sites (production + 20+ test files)
- Delete `migrate.ts` (old API config migration, zero external imports)
- Remove `LegacyConfigFormat` and `getLegacyConfig()` from storage.ts
- Remove `export * from './migrate'` from api-config/index.ts