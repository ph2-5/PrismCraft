chore: clean up outdated information

- Remove @deprecated MODEL_CAPABILITIES alias (zero usages)
- Update project_rules.md: non-null assertions debt marked as fixed
  (production code has 0 `!.`, `as unknown as` only in test files)
- Delete .ai/modules/feedback.md and security.md (modules removed)
- Console usage in production code is only in error-logger and
  logging transports (appropriate, no changes needed)