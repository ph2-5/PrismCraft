refactor: optimize StoryBeat schema and eliminate legacy debt

1. Remove 4 dead fields: _blobUrls, proElementInstances, proActions, promptLab
2. Add field grouping comments to StoryBeat schema (6 categories)
3. Migrate character → characterIds: add getBeatCharacterIds() utility,
   unify 7 read sites, mark character/characters as @deprecated
4. quick-mode.ts: STYLE_PRESETS keys from Chinese to English identifiers,
   DURATION_OPTIONS/RESOLUTION_OPTIONS changed to functions with t() labels
5. Add 18 i18n keys for style/duration/resolution labels
6. Update video-templates style values to English keys