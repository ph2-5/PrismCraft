# 画布式故事创作与分镜编辑器改造方案

> 版本：v1.0（2026-08-02）
> 状态：待评审
> 关联：用户对现有 UI 管理的整体不满意 → 项目工作台化 + 分镜画布化

---

## 一、背景与目标

### 1.1 现状问题

| # | 问题 | 现状 |
|---|------|------|
| 1 | 故事创作入口过重 | `/story` 首次进入直接弹出三模式选择（quick/standard/professional），用户被迫先选"流程长度"而非直接开始创作 |
| 2 | 三模式实质差异小 | standard 与 professional 主流程/UI 完全相同，仅差"结构分析 + 节奏规划"两个增强面板；quick 为单页快速出片。**用户决定：本次暂不动三模式**，仅改造入口 |
| 3 | 分镜编辑器是列表式 | `/storyboard` 为"左列表 + 右详情 + 底时间线"，镜头按线性列表呈现，缺乏空间感与关系表达 |
| 4 | 元素绑定不可视化 | 分镜与角色/场景/首尾帧的绑定关系只存在于表单（ElementBindingPanel）中，用户无法直观看到"哪个角色参与了哪些镜头" |

### 1.2 目标

1. **故事创作页 → 项目工作台**：像 IDE 一样"先建项目，再细致编辑"。打开 `/story` 显示项目总览（复用故事库 /stories 风格），无故事时空白 +「新建故事」按钮；新建通过弹窗（命名/风格/类型/精细度）。
2. **分镜编辑器 → 无限画布 + 节点连线**：采用无限画布 + 节点化的通用交互范式（参考 ComfyUI 等开源工具的节点工作流思想）。每个分镜是一个可自由拖动的节点卡片，角色/场景/首尾帧作为资源节点，节点间连线表达绑定与序列关系。
3. **画布与详细编辑双向联动**：点击分镜节点进入现有详细编辑；连线绑定与 ElementBindingPanel 双向同步（正向：连线→表单显示引用；反向：表单改动→画布连线更新）。

> ⚖️ **合规性声明（见第八节）**：本方案借鉴的是"无限画布 + 节点连线"这一通用交互范式与开源技术，不复制任何竞品（Seko / Joker AIx / 飙算画影等）的视觉设计、图标、文案、布局细节或素材。全部视觉与交互沿用本项目自身设计语言。

---

## 二、总体架构

```
┌─────────────────────────────────────────────────────────┐
│                   故事项目工作台 (/story)                 │
│   总览（无故事=空态+新建按钮）→ 新建弹窗 → 创作流水线      │
└─────────────────────────┬───────────────────────────────┘
                          │ 创建/打开项目
                          ▼
┌─────────────────────────────────────────────────────────┐
│              分镜无限画布编辑器 (/storyboard/:storyId)     │
│  ┌──────────┐   ┌──────────┐   ┌──────────────────┐     │
│  │ 分镜节点  │──▶│ 分镜节点  │   │ 资源节点（角色/    │     │
│  │ Beat #1  │   │ Beat #2  │   │ 场景/首尾帧/3D）  │     │
│  └────┬─────┘   └────┬─────┘   └──────────────────┘     │
│       │ 绑定连线      │ 绑定连线                           │
│       ▼              ▼                                   │
│  ┌─────────────────────────────────────────────────┐     │
│  │ 详细编辑（复用 BeatDetailView + ElementBinding）  │     │
│  └─────────────────────────────────────────────────┘     │
│  底部：时间线（节奏辅助，保留现有 BeatTimelineSection）    │
└─────────────────────────────────────────────────────────┘
```

**核心原则：连线是"视图层派生"，不新增数据模型。**

分镜与资源的绑定关系单一事实源仍是 `StoryBeat` 字段：
- `characterIds[]` / `characterVariantIds[]` → 分镜↔角色连线
- `sceneId` / `sceneTransitions[]` → 分镜↔场景连线
- `firstFrame` / `lastFrame`（含 `derivedFrom`）/ `firstFrameUrl` / `lastFrameUrl` → 分镜↔首尾帧节点
- `sequence` / `order` → 分镜↔分镜序列连线

画布与详细编辑只是同一份数据的两种视图，天然双向联动，无需数据迁移。

---

## 三、阶段 1：故事项目工作台（/story 改造）

### 3.1 交互设计

| 场景 | 行为 |
|------|------|
| 打开 `/story` 且无任何故事 | 显示项目总览空态：插图 +「新建故事」主按钮 +「加载示例」次级入口 |
| 打开 `/story` 且已有故事 | 显示故事卡片网格（复用 /stories 的 StoryCard 风格），支持搜索/排序/打开/删除 |
| 点击「新建故事」 | 弹出新建项目对话框 |
| 新建项目对话框 | 字段：**项目名称**（必填）、**故事类型**（叙事/科幻/古装…下拉）、**艺术风格**（可选，水彩/写实…）、**AI 精细度**（标准/专业，替代三模式入口，默认标准） |
| 确认创建 | 创建 Story 记录 → 回到工作台总览（新项目出现在列表，由用户点击卡片进入分镜画布编辑，避免空白项目直接落入密集编辑器） |
| 打开已有故事 | 跳转 `/storyboard/:storyId` |

### 3.2 与现有页面的关系

- **复用**：`/stories`（故事库）页面已具备搜索/筛选/复制/删除的完整能力。`/story` 工作台与其视觉风格统一，职责区分为：`/story` = "我的创作工作台"（总览 + 新建入口），`/stories` = "故事库管理"（全量管理）。两者数据同源（stories 表），避免重复建设。
- **三模式（本次不动）**：ModeSelector 从 `/story` 首屏移除，改为：新建项目弹窗中的"AI 精细度"字段，创建时写入 `PipelineConfig.aiAssistLevel`（standard 默认）。原 ModeSelector 组件保留（仍有"切换模式"按钮引用），后续用户确认后再决定删除。

### 3.3 涉及文件（预估）

| 文件 | 改动 |
|------|------|
| `src/app/story/page.tsx` | 从"直接渲染 StoryPipelineShell"改为"项目工作台 + 新建弹窗 + 有项目时进入流水线" |
| `src/modules/novel/presentation/StoryPipelineShell.tsx` | 支持 `initialStory` prop（已创建项目直接进入流水线，跳过 ModeSelector） |
| 新建 `src/app/story/StoryWorkbench.tsx` | 项目总览组件（空态/网格/新建弹窗） |
| 新建 `src/app/story/NewStoryDialog.tsx` | 新建项目弹窗 |
| `src/modules/storyboard`（useCreateStory 等） | 确认创建/查询 API 是否已具备，缺失则补充 |

### 3.4 验证

- typecheck + lint + 相关测试
- 手工：无故事 → 空态；新建 → 弹窗 → 进入分镜画布；已有故事 → 网格打开

---

## 四、阶段 2：分镜无限画布（/storyboard 改造）

### 4.1 技术选型

**采用 `@xyflow/react`（React Flow v12）**，理由：

| 维度 | React Flow | 自研 |
|------|-----------|------|
| 无限画布（平移/缩放/框选/迷你地图） | ✅ 内置 | 需自研（pan/zoom 数学 + 手势） |
| 节点拖拽/连线/自定义节点 | ✅ 成熟 API | 工作量大 |
| 性能（数千节点虚拟化） | ✅ 内置 viewport 优化 | 需自研 |
| 维护成本 | 社区活跃（xyflow 团队维护） | 高 |
| 体积 | ~40KB gzip（可 tree-shake） | - |

> 备选：`@xyflow/react` 已支持 React 19（本项目用 React 19.2），需在实施前验证兼容性。

### 4.2 画布布局

```
┌──────────────────────────────────────────────────────────────┐
│ [工具栏] 框选/连线开关 | 适应视图 | 迷你地图开关 | 自动布局     │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│   ┌─────────┐   ┌─────────┐   ┌─────────┐   ┌─────────┐     │
│   │ Beat #1 │──▶│ Beat #2 │──▶│ Beat #3 │──▶│ Beat #4 │     │
│   │ 缩略图   │   │ 缩略图   │   │ 缩略图   │   │ 缩略图   │     │
│   └──┬──┬───┘   └──┬──┬───┘   └──┬──┬───┘   └──┬──┬───┘     │
│      │  │          │  │          │  │          │  │          │
│  角色A│  │场景X  角色B│  │场景Y  角色A│  │场景X  角色B│  │场景Z   │
│      ▼  ▼          ▼  ▼          ▼  ▼          ▼  ▼          │
│  ┌──────────┐  ┌──────────┐   ┌─────────────────────────┐     │
│  │ 角色节点   │  │ 场景节点   │   │ 首尾帧/关键帧节点（可折叠）│     │
│  │ 角色A     │  │ 场景X     │   │ Beat#1 last→#2 first    │     │
│  └──────────┘  └──────────┘   └─────────────────────────┘     │
│                                                              │
│  [底部] 时间线（节奏辅助，保留 BeatTimelineSection）           │
└──────────────────────────────────────────────────────────────┘
```

### 4.3 节点类型设计

| 节点类型 | 数据来源 | 显示内容 | 交互 |
|----------|---------|---------|------|
| **BeatNode（分镜节点）** | `StoryBeat[]` | 序号 + 首帧缩略图 + 景别/时长 + 生成状态徽标 + prompt 片段 | 单击选中；**双击/点击编辑按钮 → 打开右侧详细编辑**；拖动可重排（拖到另一个 Beat 前/后 = 序列调整） |
| **CharacterNode（角色节点）** | `characters[]`（绑定关系来自所有 beat 的 `characterIds` 并集） | 头像 + 名称 + 参与镜头数 | 点击 → 高亮所有引用它的分镜连线；拖动连线到 Beat = 绑定 |
| **SceneNode（场景节点）** | `scenes[]`（来自 `sceneId` 并集） | 场景图 + 名称 | 同上 |
| **FrameNode（首尾帧/关键帧节点）** | `StoryBeatKeyframe[]` / `firstFrame` / `lastFrame` | 帧缩略图 + 来源标记（derivedFrom） | 折叠/展开；连线表达帧衔接 |
| **Blockout3DNode（3D 导演台节点）** | `StoryBeat.blockout3D` | 3D 白模预览（复用 blockout-3d 渲染） | 点击 → 打开 3D 编辑器；作为镜头构图参考节点 |

**连线类型（Handle 设计）**：

| 连线 | 起点/终点 | 语义 | 数据映射 |
|------|----------|------|----------|
| 序列连线 | Beat → Beat | 镜头顺序（#1 → #2 → #3） | `sequence` 派生，断连/重连 = 重排 order |
| 角色绑定 | Character → Beat | 该角色出现在此镜头 | `characterIds`（增删连线 = 更新数组） |
| 场景绑定 | Scene → Beat | 该镜头主场景/转场 | `sceneId` / `sceneTransitions` |
| 帧衔接 | Beat A.lastFrame → Beat B.firstFrame | 首尾帧引用 | A.lastFrame.derivedFrom / B.firstFrame.derivedFrom |

### 4.4 画布 ↔ 详细编辑双向联动（用户核心需求）

**数据流模型：单一事实源 = `StoryBeat` 字段；画布连线与 ElementBindingPanel 均为视图。**

| 方向 | 场景 | 实现 |
|------|------|------|
| **画布 → 表单** | 用户把"角色A"节点连线到"分镜#2" | 更新 `beat.characterIds`（add）→ ElementBindingPanel 自动显示"已引用：角色A"（现有 `onUpdateBeat` 通路，无需改表单） |
| **画布 → 表单** | 断开某条绑定连线 | 更新 `beat.characterIds`（remove）→ 表单同步移除 |
| **表单 → 画布** | 用户在 ElementBindingPanel 勾选/取消"角色A" | `onUpdateBeat` → 画布通过 `characterIds` 派生重新计算连线，新增/移除连线 |
| **反向引用** | 点击"角色A"节点 | 画布高亮所有 `characterIds.includes(A)` 的分镜；**并在右侧信息面板显示"被以下 N 个镜头引用"列表**（对应 storyboard 内已有 `characterAnchors` 一致性逻辑） |
| **首尾帧联动** | 用户在某分镜详细编辑中重新生成关键帧/首尾帧 | `firstFrameUrl`/`lastFrameUrl`/`derivedFrom` 更新 → FrameNode 缩略图同步刷新 |

**关键点**：
- 现有 `BeatPromptPanel` 已有"提示词元素绑定高亮"（Task 2B.11），画布节点高亮逻辑可复用其"计算 beat 绑定的角色/场景"模式。
- 连线增删 = 调用现有 `onUpdateBeat`/`updateBeat`，不引入新的写路径，风险可控。

### 4.5 首尾帧与分镜引用设计（详细）

1. **帧衔接连线**：允许用户显式连线 A 的 `lastFrame` → B 的 `firstFrame`，表示"镜头 B 以镜头 A 的尾帧起手"。写入 B.firstFrame.derivedFrom = A.lastFrame 的引用（schema 已支持 `derivedFrom`）。
2. **自动建议**：生成帧对（frame pair）后，若 B.firstFrame 为空且 A.lastFrame 存在，画布给出"衔接建议"虚线连线，用户确认后落库。
3. **引用反查**：任意资源节点（角色/场景/帧）的详情面板展示"引用清单"：`被哪些分镜引用 / 作为谁的尾帧`。数据从所有 beat 派生，无额外存储。
4. **删除保护**：删除角色/场景/帧前，通过引用反查提示"该元素被 N 个分镜引用"，与现有删除级联校验（delete-character-cascade）一致。

### 4.6 时间线与 3D 导演台

- **底部时间线（BeatTimelineSection）保留**：作为"节奏辅助视图"，与画布双视图联动（时间线点击某 beat → 画布定位并高亮对应节点）。二者共用同一 `beats` 数据源。
- **3D 导演台**：`blockout-3d` 模块已有 MannequinControls/CameraPathEditor（3D 白盒排布 + 相机轨迹）。在画布中作为 Blockout3DNode 嵌入，点击进入 3D 编辑器（复用现有面板），输出 `blockout3D` 字段。

### 4.7 涉及文件（预估）

| 区域 | 改动 |
|------|------|
| 依赖 | 新增 `@xyflow/react` |
| 新建 `src/modules/storyboard/canvas/` | `StoryboardCanvas.tsx`（画布主体）、`nodes/BeatNode.tsx`、`nodes/ResourceNode.tsx`、`nodes/FrameNode.tsx`、`edges/`（连线类型）、`hooks/useCanvasBindings.ts`（派生连线）、`layout/auto-layout.ts` |
| 改造 `ProfessionalModeEditor.tsx` | 中栏 `BeatListView` → `StoryboardCanvas`（保留右侧 `BeatDetailView` + 底部时间线） |
| `ElementBindingPanel.tsx` / `BeatPromptPanel.tsx` | 无需改数据逻辑；仅确认通过 `onUpdateBeat` 更新后可触发画布重新派生 |
| `use-story-page.ts` | 暴露 `updateBeat` 等写方法（已有），补充"引用反查"派生查询 |

### 4.8 验证

- typecheck + lint + 相关测试（storyboard 模块 99+ 测试不回归）
- 手工：画布拖拽/缩放/框选；连线增删 ↔ 表单同步；首尾帧衔接；删除保护提示
- 性能：100+ beat 时画布流畅（React Flow 虚拟化验证）

---

## 五、阶段 3：串联闭环

1. `/story`（工作台）→ `/storyboard/:storyId`（画布）→ 生成视频 → 回 `/stories` 管理
2. `/stories` 打开故事 → `/storyboard/:storyId`（保持现有跳转，画布替换列表后自然升级）
3. 故事库/工作台共用 stories 数据源，不重复

---

## 六、实施计划

| 步骤 | 内容 | 依赖 | 验证 |
|------|------|------|------|
| S1 | 阶段 1：项目工作台 + 新建弹窗（改动集中、风险低） | 无 | typecheck/lint/测试 + 手工 |
| S2 | 阶段 2 前置：引入 @xyflow/react，验证 React 19 兼容性 | S1 | 最小 demo 组件跑通 |
| S3 | 阶段 2：BeatNode 画布替换 BeatListView（含拖拽重排/选中） | S2 | 现有 99+ 测试不回归 |
| S4 | 阶段 2：资源节点 + 绑定连线（角色/场景/帧），双向联动 | S3 | 双向同步测试 |
| S5 | 阶段 2：首尾帧衔接 + 引用反查 + 删除保护 | S4 | 专项测试 |
| S6 | 阶段 3：三端闭环联调 + 性能验证 | S5 | 全量测试 |

---

## 七、风险与决策点

| # | 风险/决策 | 应对 |
|---|----------|------|
| 1 | @xyflow/react 与 React 19 兼容性 | S2 先行验证；不兼容则评估 @xyflow 版本或 React Flow v11 |
| 2 | 画布改造影响现有 99+ storyboard 测试 | 渐进替换：先并列保留 BeatListView（开关切换），测试全绿后再移除旧视图 |
| 3 | 连线派生逻辑复杂度 | 收敛到 `useCanvasBindings` 单一 hook，纯函数派生 + 单元测试覆盖 |
| 4 | 三模式去留（用户未定） | 本次仅移除首屏入口（降级为弹窗字段），组件保留，待用户确认后决定 |
| 5 | 性能（大量 beat + 资源节点） | React Flow 虚拟化 + 连线按需渲染（折叠资源节点） |

---

## 八、验收标准

1. `/story` 打开为项目工作台：无故事空态 +「新建故事」弹窗，创建后直达画布编辑
2. `/storyboard` 中栏为无限画布：分镜节点可拖动/缩放/框选，序列连线可重排
3. 角色/场景节点连线 ↔ ElementBindingPanel 双向同步（正反两向）
4. 首尾帧衔接连线可建立/断开，derivedFrom 正确写入
5. 任意资源节点可反查"被引用分镜清单"，删除有保护提示
6. 底部时间线与画布双视图联动
7. 全部既有测试通过，lint/typecheck 零告警

---

## 九、合规性声明（侵权风险审查）

> 本节为实施时的强制约束，所有参与方（含 AI 辅助编码）必须遵守。

### 9.1 基本原则

**借鉴"思想/交互范式"，不复制"表达/视觉呈现"。**
著作权法保护的是作品的**具体表达**（视觉设计、图标、文案、代码、素材），不保护**思想、方法、交互范式**（无限画布、节点连线、拖拽工作流）。因此：

| 允许 | 禁止 |
|------|------|
| 采用"无限画布 + 节点连线"的通用交互范式 | 复制竞品（Seko / Joker AIx / 飙算画影等）的界面截图、配色方案、图标 |
| 参考 ComfyUI 等开源项目的节点工作流**思想** | 直接套用竞品的文案、按钮文字、引导语 |
| 使用 MIT 开源库 @xyflow/react 的 API | 在 UI 中展示竞品 Logo、名称、水印 |
| 沿用本项目自身设计语言（design-preview.html 风格） | 像素级复刻任何第三方界面的布局细节 |

### 9.2 技术选型许可

| 依赖 | 协议 | 可商用 |
|------|------|--------|
| @xyflow/react（React Flow） | MIT | ✅ 自由商用，需保留版权声明 |
| 项目现有依赖（react/zod/zustand 等） | MIT/BSD 等 | ✅ 已在使用 |

### 9.3 实施检查清单

- [ ] 画布节点/连线的视觉样式一律使用项目现有 CSS 变量与组件（card/btn/badge 等），不复刻任何第三方样式
- [ ] 图标使用项目已有的 lucide-react 图标库，不引入/描摹第三方图形
- [ ] 文案（空态引导、节点标题、提示语）为项目原创撰写，可复用现有 i18n key，不摘抄竞品文案
- [ ] 3D 导演台节点复用项目自研 blockout-3d 模块，不调用/捆绑任何第三方 3D 编辑器
- [ ] 文档与代码中不出现竞品名称/Logo/素材（本方案文档中仅作交互模式名词提及，实施代码中不引用）
- [ ] @xyflow/react 安装后保留其 LICENSE 声明（依赖管理默认行为）

### 9.4 边界说明

- 本方案的创新点（分镜↔资源双向联动、首尾帧衔接、引用反查）均为项目自有设计，不存在借鉴第三方的问题。
- 若后续需要参考具体竞品的**交互流程**（非视觉），仅以文字描述内部评审，不复制其界面素材。
