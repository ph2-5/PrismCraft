# 【学习工作赛道】PrismCraft — 智能动画创作工作台（初赛 Demo 提交）

> **赛道**：通用赛道 / 02 学习工作 — 造个新解法
> **版本**：v0.12.1
> **Demo 下载**：见文末「评委快速体验」
> **报名帖**：[本地桌面应用，把角色和场景从提示词里拎出来做结构化](https://forum.trae.cn/t/topic/33047)

---

## 一、产品介绍

### 创意名称
**PrismCraft — 智能动画创作工作台**

### 一句话定位
**把专业工作室的动画创作流程，搬到普通人的电脑上。**

### 解决什么问题
做系列视频内容时：
- 同一个角色的提示词要反复写、角色图要反复上传
- 改一次设定要同步十几个分镜
- 素材散落在各家平台，版本混乱
- 重做一个镜头可能找不到之前的参考

市面上的工具都只解决"生成"，不解决"管理"。真正的痛点不在"会不会生成"，而在"能不能系统化管理角色、场景、分镜和元素绑定"。

### 产品形态
桌面应用（Electron + React + TypeScript + better-sqlite3），所有数据存在本地 SQLite 数据库，可整体导出、备份、迁移，零云端依赖。

---

## 二、核心功能展示

> 📌 **以下截图待补充**（占位符：`[截图: xxx]`）

### 1. 主页 — 项目工作台

`[截图: 主页 dashboard，展示项目概况、快捷入口、最近任务]`

主页作为创作工作台，集中展示项目状态、快捷入口和最近任务，对齐 design-preview 设计稿。

### 2. 角色管理 — 结构化管理 + 一致性锚定

`[截图: 角色管理页面，展示角色列表 + 编辑面板]`

- 角色卡片化管理，支持头像 / 生成图 / 参考图 / 服装库
- AI 请求预览卡片：展示最终发送给大模型的完整请求内容（Prompt / 模型配置 / 参考图片 / 关联分镜 / JSON）
- 特征锚定：把角色的关键特征（发色 / 服装 / 表情）固化为可复用模板
- 删除被分镜引用的角色时自动拦截

### 3. 场景管理 — 状态化场景库

`[截图: 场景管理页面，展示场景列表 + 编辑面板]`

- 场景状态库（白天 / 黑夜 / 雨天等）
- 与角色管理一致的结构化模式

### 4. 分镜编辑 — 元素绑定 + 一致性检查

`[截图: 分镜编辑页面，展示 beat 列表 + 元素绑定面板]`

- 分镜卡片化（timeline-card）
- 元素绑定面板：把角色 / 场景 / 道具绑定到具体分镜
- 一致性检查：自动检测分镜间的元素是否冲突
- Prompt 编辑器：浮动球式 prompt 编辑，支持模板变量

### 5. 故事创作 — AI 自动拆解

`[截图: 故事页面，展示故事大纲 + 自动拆解为分镜]`

- AI 管道：从故事大纲自动拆解为分镜
- 支持 OpenAI / Claude / Gemini / 智谱等多家文本模型
- 自动生成分镜 prompt，绑定默认元素

### 6. 视频任务管理 — CQRS 模式 + 多模型聚合

`[截图: 视频任务管理页面，展示任务列表 + 任务详情对话框]`

- 13 家 AI 视频提供商统一接口（Kling / Runway / Pika / Luma / MiniMax 等）
- CQRS 模式：State / Queries / Commands / Polling 分离
- 任务状态机：transition-guard 防止非法状态转换
- 重复检测：相似 prompt + 参考图自动复用已有任务，避免重复计费
- 任务恢复：崩溃后自动恢复未完成任务
- 视频缓存：自动缓存生成结果，避免重复请求

### 7. 素材库 — 统一管理 + 导入导出

`[截图: 素材库页面，展示角色库 / 场景库 / 分镜库 / 合集]`

- 多 tab 分类：全部素材 / 角色库 / 场景库 / 分镜库 / 道具 / 合集 / 媒体资产
- 搜索 + 标签筛选
- 支持 .asa 项目导出导入

### 8. 设置 — API 密钥系统级加密

`[截图: 设置页面，展示 API 提供商配置 + 模型选择]`

- 13 家 AI 提供商配置
- API Key 系统级加密存储（macOS Keychain / Windows Credential Manager）
- 模型能力自适应：自动检测模型支持的能力（首尾帧 / 参考图 / 时长等）
- 声明式 JSON 插件：零代码接入新提供商

---

## 三、技术亮点（截图展示）

### 1. 工程规模

`[截图: 仓库统计 — 20,000+ 行代码 / 9 业务模块 / 13 AI 提供商]`

| 指标 | 数值 |
|------|------|
| 代码行数 | 20,000+ 行 TypeScript |
| 业务模块 | 9 个（故事 / 角色 / 场景 / 分镜 / 视频任务 / 视频恢复 / 视频缓存 / 素材 / 同步） |
| AI 提供商 | 13 家原生 + 声明式 JSON 插件扩展 |
| 单元测试 | 4,801 个 |
| E2E 测试 | 126 个 |
| 回归防护规则 | 168 条 |
| DDD 分层 | 4 层（domain / shared-logic / shared / infrastructure + modules） |

### 2. 架构图

`[截图: 整体系统架构图 — docs/architecture/diagrams/01-overall-system-architecture.png]`

`[截图: 依赖方向图 — docs/architecture/diagrams/dependency-direction.png]`

### 3. 测试报告

`[截图: npm test 输出 — 4,801/4,801 passed]`

`[截图: playwright e2e 输出 — 126/126 passed]`

---

## 四、Trae 协作叙事

### 项目由来

- **第一阶段**：最初用 OpenClaw 搭建原型，因打包白屏和成本问题放弃
- **第二阶段**：转到 Trae Solo，多轮迭代、优化和重构，形成当前版本
- **核心模式**：Trae Solo 生成初版代码 → 人工调试完善 → 抽取规则固化防止回归

### 8 个月持续协作的关键机制

PrismCraft 不是"用 AI 写了一个 Demo"，而是**用 Trae 持续维护了 8 个月的真实工程项目**。这背后的关键机制：

#### 1. 分层规则加载
- Layer 0：始终加载的 quick-start.md（~1,500 tokens）
- Layer 1：任务触发加载（architecture-rules / testing-rules）
- Layer 2：按需查找（regression-guards / MODULE.md / contract.json）

解决的问题：AI 上下文有限，无法一次加载所有规则。按需加载让 AI 用最少上下文做最正确决策。

#### 2. contract 驱动开发
- 每个模块有 `MODULE.md` 定义边界
- 每个子域有 `contract.json` 定义 `publicAPI` 和 `invariants`
- 防止 AI 跨层违规、幻觉 API、重复造轮子

#### 3. 回归防护体系
- 168 条回归规则，每条对应一个真实 bug
- ESLint `no-direct-db-ipc` 等自定义规则强制执行
- 架构扫描脚本 `check-architecture.mjs` 检测依赖方向

#### 4. 会话状态传递
- `.ai/session-notes.md`：追加式会话日志，新会话可恢复上下文
- `.ai/work-claims.md`：工作声明机制，防止多会话冲突
- 防幻觉检查点：调用 DI token / shared-logic / API 路由前必须先读对应文件

### 协作成果

| 协作维度 | 成果 |
|---------|------|
| 持续时间 | 8 个月 |
| 代码规模 | 20,000+ 行 |
| 测试覆盖 | 4,801 单测 + 126 E2E |
| 回归规则 | 168 条 |
| 架构层级 | DDD 4 层 |
| 模块数 | 9 个业务模块 + shared-logic |

**这不是"用 AI 写代码"，而是"建立了一套让 AI 能长期稳定维护大型项目的工程基础设施"。**

---

## 五、评委快速体验

### Demo 下载

> 📌 **下载链接待补充**

- **Windows 安装包**（113 MB）：`PrismCraft Setup 0.12.1.exe`
- **运行环境**：Windows 10/11 x64
- **未签名提示**：首次运行 Windows SmartScreen 可能拦截，点击"更多信息 → 仍要运行"即可

### 快速体验路径（5 分钟）

1. **安装并启动** PrismCraft
2. **首次启动**：会引导配置至少 1 个 AI 提供商（推荐先用智谱 AI 或 OpenAI 测试文本生成）
3. **创建角色**：进入"角色"页面 → 点击"+ 创建新角色" → 填写名称 → 点击"生成角色图"
4. **创建场景**：进入"场景"页面 → 创建一个场景
5. **创建分镜**：进入"分镜"页面 → 创建 2-3 个分镜 → 绑定刚才的角色和场景
6. **生成视频**（可选，需要视频模型 API Key）：选中分镜 → 点击"生成视频"

### 体验重点

- ✅ **本地优先**：所有数据存在本地 SQLite，关闭应用再打开数据仍在
- ✅ **结构化管理**：改一次角色信息，所有引用该角色的分镜自动同步
- ✅ **多模型聚合**：13 家 AI 提供商统一接口，无需学习各家参数体系
- ✅ **AI 请求预览**：角色编辑页可查看"最终发送给大模型的完整请求内容"
- ✅ **重复检测**：相似 prompt + 参考图会自动复用已有任务，避免重复计费

---

## 六、后续规划

### 1.0 发布标准
"现有功能稳定可用" — 当前 v0.12.1 已达成：
- ✅ typecheck 通过
- ✅ lint 0 errors
- ✅ 4,801/4,801 单测通过
- ✅ 126/126 E2E 通过
- ✅ 已知 bug 全部修复（见 docs/bug-audit-report.md）

### 复赛阶段计划
- macOS / Linux 安装包
- Windows 代码签名（消除 SmartScreen 警告）
- Demo 视频录制（2-3 分钟全流程演示）
- 用户文档完善

---

## 七、联系方式

- **项目仓库**：私人仓库（不开源，可联系申请访问）
- **问题反馈**：通过社区私信
- **社区主页**：[u590909302648505](https://forum.trae.cn/u/u590909302648505)

---

## 附录：技术栈

| 层 | 技术 |
|----|------|
| 运行时 | Electron 41 |
| 前端 | React 19 + TypeScript 6 strict + Tailwind CSS 4 |
| 状态 | Zustand + React Query |
| 数据库 | better-sqlite3（本地 SQLite） |
| 构建 | Vite + electron-builder |
| 测试 | Vitest（单测）+ Playwright（E2E） |
| 架构 | DDD 分层 + DI Container + CQRS |
| 安全 | 系统级 API Key 加密 + SQL 参数化 + vm 沙箱插件隔离 + SSRF 防护 |
